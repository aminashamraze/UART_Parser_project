# UART_Parser_project
UART command parser contains a list of functions that are called when parsing and storing a UART command using fixed size buffers, enums and structs.


Use Updated_uart.c because the UART.c may have some safety issues that i did not consider first. 
Updated version is supposed to be the clean version of this project.
